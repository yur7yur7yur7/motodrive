# IDE
.idea/

# Playwright scratch
.playwright-cli/

# Local serve logs (started via npm run dev / python -m http.server)
serve.log
serve.err.log

# Source photos & videos — only compressed WebP in img/products/ and
# transcoded mp4/webm in photo/web/ are shipped.
# Cyrillic-named source directories (working set) and original mp4 sources
# stay local; the transcoded outputs go to the repo.
photo/*.mp4
photo/[^w]*/

# OS
.DS_Store
Thumbs.db
